goog.declareModuleId('os.CameraMode');

/**
 * Modes for setting the camera position when the application is launched.
 * @enum {string}
 */
const CameraMode = {
  DEFAULT: 'default',
  FIXED: 'fixed',
  LAST: 'last'
};

export default CameraMode;
