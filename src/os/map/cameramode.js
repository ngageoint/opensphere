goog.module('os.CameraMode');

/**
 * Modes for setting the camera position when the application is launched.
 * @enum {string}
 */
exports = {
  DEFAULT: 'default',
  FIXED: 'fixed',
  LAST: 'last'
};
