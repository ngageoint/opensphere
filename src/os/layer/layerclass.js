goog.declareModuleId('os.layer.LayerClass');

/**
 * Layer class names used with os.registerClass and os.instanceOf.
 * @enum {string}
 */
const LayerClass = {
  GROUP: 'os.layer.LayerGroup',
  TILE: 'os.layer.Tile',
  VECTOR: 'os.layer.Vector'
};

export default LayerClass;
