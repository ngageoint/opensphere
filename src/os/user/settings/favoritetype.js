goog.declareModuleId('os.user.settings.FavoriteType');

/**
 * @enum {string}
 */
const FavoriteType = {
  DESCRIPTOR: 'descriptor',
  FOLDER: 'folder',
  MISSING: 'missing',
  SEARCH: 'search.v1'
};

export default FavoriteType;
