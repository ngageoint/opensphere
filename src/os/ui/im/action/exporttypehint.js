goog.declareModuleId('os.im.action.filter.ExportTypeHint');

/**
 * Type hints used in exported filter actions to identify how the type should be used.
 * @enum {string}
 */
const ExportTypeHint = {
  FILTERABLE: 'filterable',
  EXACT: 'exact'
};

export default ExportTypeHint;
