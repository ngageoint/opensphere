goog.declareModuleId('os.state.v2.FilterTag');

/**
 * XML tags for filter state
 * @enum {string}
 * @const
 */
const FilterTag = {
  FILTERS: 'filters',
  FILTER: 'filter'
};

export default FilterTag;
