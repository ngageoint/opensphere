goog.declareModuleId('os.config.ConfigType');

/**
 * Configuration types for settings
 * @enum {string}
 */
const ConfigType = {
  CONFIG: 'admin',
  PREFERENCE: 'user',
  OVERRIDES: 'overrides'
};

export default ConfigType;
